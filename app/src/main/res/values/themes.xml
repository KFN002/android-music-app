<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Primary app theme used by manifest. AppCompat-based for maximum XML theme compatibility. -->
    <style name="Theme.MinimalMusic" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
    </style>

    <!-- Compatibility aliases for stale/generated references in old builds/caches. -->
    <style name="Theme.Material3.DayNight" parent="Theme.AppCompat.DayNight" />
    <style name="Theme.Material3.DayNight.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar" />
    <style name="Theme.MaterialComponents.DayNight.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar" />
</resources>
